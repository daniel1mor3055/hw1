go over permutes!

investigate einsum function

migrate torchtext to datasets

remove dummy stuff

wandb only once

model configuration for pretrain + finetune should be the same

remove select minimizing dataset

context manager pickle get_vocab()

prints to logs